# Thư mục Scripts

Thư mục này chứa các kịch bản tự động hóa (PowerShell/Batch) hỗ trợ cho quá trình Build, Test, và Deploy:
- Cài đặt / gỡ cài đặt Driver nhanh.
- Thiết lập chế độ Test Signing (`bcdedit`).
- Tạo chứng chỉ số (Certificate) giả lập.
